name: Release

on:
  push:
    branches:
      - 'main'
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  test:
    name: test
    runs-on: windows-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: '>=1.24'
          check-latest: true
      - name: Ensure MinGW GCC in PATH
        shell: powershell
        run: |
          if (Test-Path "C:\msys64\mingw64\bin\gcc.exe") {
            "C:\msys64\mingw64\bin" | Out-File -FilePath $env:GITHUB_PATH -Encoding utf8 -Append
          } elseif (Test-Path "C:\Strawberry\c\bin\gcc.exe") {
            "C:\Strawberry\c\bin" | Out-File -FilePath $env:GITHUB_PATH -Encoding utf8 -Append
          }
      - name: Run Tests
        shell: powershell
        run: go test -v ./...
      - name: Run Race Tests
        shell: powershell
        run: |
          if (Test-Path "C:\msys64\mingw64\bin") { $env:PATH = "C:\msys64\mingw64\bin;" + $env:PATH }
          elseif (Test-Path "C:\Strawberry\c\bin") { $env:PATH = "C:\Strawberry\c\bin;" + $env:PATH }
          $env:CGO_ENABLED = "1"
          go test -race -v ./...
      - name: Run Vet
        shell: powershell
        run: go vet ./...

  build-and-release:
    name: build-and-release
    needs: test
    runs-on: windows-latest
    permissions:
      contents: write
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: '>=1.24'
          check-latest: true
      - name: Build Windows x64 Executable
        shell: powershell
        run: |
          $ErrorActionPreference = 'Stop'
          $env:GOOS = "windows"
          $env:GOARCH = "amd64"
          go build -ldflags="-s -w" -o gopass.exe ./cmd/gopass
          $version = if ($env:GITHUB_REF_NAME -match '^v') { $env:GITHUB_REF_NAME } else { 'v1.6.8' }
          $versioned = "gopass_" + $version.TrimStart('v') + ".exe"
          Copy-Item gopass.exe $versioned
          Get-Item gopass.exe, $versioned | Select-Object Name, Length
      - name: Upload Windows build (main validation)
        if: github.ref_type == 'branch' && github.ref_name == 'main'
        uses: actions/upload-artifact@v4
        with:
          name: gopass-windows-main-${{ github.sha }}
          path: gopass*.exe
          if-no-files-found: error
      - name: GitHub Release
        if: startsWith(github.ref, 'refs/tags/v') || (github.ref_type == 'branch' && github.ref_name == 'main')
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_type == 'tag' && github.ref_name || 'v1.6.8' }}
          name: GoPass ${{ github.ref_type == 'tag' && github.ref_name || 'v1.6.8' }}
          body: |
            GoPass ${{ github.ref_type == 'tag' && github.ref_name || 'v1.6.8' }}

            - Process-whitelist transparent IPv4 TCP interception
            - Only whitelisted applications use the configured upstream SOCKS5/HTTP proxy
            - Non-whitelisted applications remain on the normal Windows direct path
            - Minimal Web UI: status, process whitelist, upstream settings, TProxy Buffer Size
            - Live process panel shows proxy/direct status, PID, connection count and target IP:port
            - TProxy Buffer Size: 32 KB–1 MB, default 256 KB
            - No GeoIP/GeoSite, DNS relay/Fake-IP, SNI, UDP/QUIC, automatic routing or route probing
          files: |
            gopass_1.6.8.exe
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - name: Create release tag after main validation
        if: github.ref_type == 'branch' && github.ref_name == 'main'
        shell: powershell
        run: |
          $ErrorActionPreference = 'Stop'
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          if (git ls-remote --exit-code --tags origin refs/tags/v1.6.8) {
            Write-Host 'v1.6.8 already exists; nothing to tag.'
            exit 0
          }
          git tag -a v1.6.8 -m 'GoPass v1.6.8' $env:GITHUB_SHA
          git push origin v1.6.8
