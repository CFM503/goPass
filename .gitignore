# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib
*.sys

# Exception: embedded DLL for single-file distribution
!internal/engine/windivert/WinDivert.dll

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
# vendor/

# Workspace file
go.work
go.work.sum

# GoPass specific
config.json
config.json.tmp
config.json.corrupt
routes_history.json*
release/

# Geo rule files (optional disk overrides, downloaded via .tools\download.ps1 or UI "Update Rule Files")
geosite.dat
geoip.dat
.tools/*.dat

# Embedded rule snapshots ARE tracked (internal/engine/rules/*.txt) — do not ignore.
